import 'package:flutter/material.dart';

class AgoraCorners {
  static const Radius noRound = Radius.circular(0);
  static const Radius rounded2 = Radius.circular(2);
  static const Radius rounded = Radius.circular(defaultRadius);
  static const Radius rounded12 = Radius.circular(12);
  static const Radius rounded42 = Radius.circular(42);
  static const Radius rounded50 = Radius.circular(50);
  static const Radius round = Radius.circular(123456789);
  static const double defaultRadius = 5;
  static BorderRadius borderRounded20 = BorderRadius.circular(20);
}
