import 'package:flutter/material.dart';

class AgoraCorners {
  static const Radius noRound = Radius.circular(0);
  static const Radius rounded50 = Radius.circular(50);
  static BorderRadius borderRound = BorderRadius.circular(123456789);
}
