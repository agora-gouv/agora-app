import 'package:flutter/material.dart';

class AgoraCorners {
  static const Radius rounded2 = Radius.circular(2);
  static const Radius rounded = Radius.circular(5);
  static const Radius round = Radius.circular(123456789);
}
